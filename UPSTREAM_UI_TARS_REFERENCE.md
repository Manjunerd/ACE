# Upstream reference

The supplied UI-TARS Desktop archive is kept outside this Python project as the upstream reference. Relevant inspected areas include `multimodal/gui-agent/action-parser`, `multimodal/gui-agent/operator-aio`, `multimodal/gui-agent/operator-browser`, and `multimodal/gui-agent/agent-sdk`. The Python project mirrors the UI-TARS-style single-action loop and action grammar while using native Windows Python operators.
